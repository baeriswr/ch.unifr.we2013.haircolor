+<h1>Ingredients</h1>

<div class="search">


  <%= form_tag search_ingredients_path, :method => "get" do %>
      <%= label_tag(:q, "Search:") %>
      <%= text_field_tag :search, params[:search] %>
      <%= submit_tag "Search", :name => nil %>
  <% end %>

</div>

+<%= render :partial => "ingredient", :collection => @ingredients %>

<%= content_for :head, auto_discovery_link_tag(:atom, ingredients_url(format: "atom")) %>

