<% provide(:title, 'Ingredient') %>
<div class="container">
  <div class="text-center">
	 <h1><%= @ingredient.inci_name %></h1>
  </div>
</div>

<% if admin_signed_in? %>
  <div class="adminview">
    <p>
      <%= link_to 'Edit ingredient', edit_ingredient_path %>
    </p>

    <p>
      <%= link_to 'Delete ingredient', ingredient_path, method: :delete %>
    </p>

  </div>
<% end %>

<%= render @ingredient %>
